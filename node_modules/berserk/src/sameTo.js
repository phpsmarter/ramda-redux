/**
 * A function return a unit what pass to it
 * @function
 * @params  {any}
 * @return {any}
 * @example
 * sameTo('hello world!')
 * // return 'hello world!'
 * */
const sameTo = (arg) => arg

module.exports= sameTo