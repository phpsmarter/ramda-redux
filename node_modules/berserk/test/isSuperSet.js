const assert = require('assert')
const j = require('../index')

describe(
	`isSupperSet function`,
	() => it(
		'judge is the set is another set\'s superset',
		() => assert.equal(
			j.isSuperSet(
				new Set([1, {a: 2}, 3]),
				new Set([1, {a: 2}, 3, 4])
			),
			true
		)
	)
)