const Pair = require('../index').Pair
const assert = require('assert')

describe(
	'midFlow function',
	() => it(
		'the ctx must be a object and merge after every middleware called',
		() => assert.deepEqual(
			Pair('name', 'jituanlin'),
			['name', 'jituanlin']
		)
	)
)