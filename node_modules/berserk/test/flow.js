const midFlow = require('../index').flow
const R =require('ramda')
const assert = require('assert')

describe(
	'flow function',
	() => it(
		'the flow function must accept a data and some function,execute function chain by chain',
		() => assert.deepEqual(
			midFlow(
				1,
				R.inc,
				R.inc
			),
			3
		)
	)
)